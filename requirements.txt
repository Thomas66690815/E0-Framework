# E₀ Framework — Python Dependencies
#
# The canonical dependency specification is in pyproject.toml.
# This file lists the minimal runtime dependencies.
#
# Core (required):
numpy

# Optional — LLM integration:
# openai

# Optional — full feature set:
# pyyaml
# duckdb
transformers
scipy

# Optional — chess engine:
chess

# Optional — REST API server:
# fastapi
# uvicorn
# python-multipart
