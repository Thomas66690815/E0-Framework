# E₀ Framework — Python Dependencies
#
# e0_core/ requires NO external dependencies.
# It runs on any Python >= 3.8.
#
# e0_middleware/ has three tiers:
#
#   Tier 1 — Simulation mode (no API key, no GPU):
#     No dependencies needed. Works out of the box.
#
#   Tier 2 — API mode (recommended, Together AI / OpenAI):
#     pip install openai
#
#   Tier 3 — Local models (CPU, no GPU required):
#     pip install -r requirements.txt

# API mode (recommended)
openai

# Potential & Helmholtz decomposition (graph Laplacian solver)
numpy

# Keimzelle — config parsing
pyyaml

# v4 Network — dialog persistence
duckdb

# Local model mode (optional)
torch --index-url https://download.pytorch.org/whl/cpu
transformers
