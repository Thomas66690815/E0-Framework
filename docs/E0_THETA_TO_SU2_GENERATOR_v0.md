# E₀ Θ → SU(2) Generator Bridge v0

**Status:** Structural derivation (critical missing link)  
**Date:** 2026-03-24  
**Purpose:** Derive how the phase structure Θ emerging in E₀ induces a generator that naturally lifts the amplitude from U(1) to SU(2).

---

## 1. Why this document exists

We now have:

- Internal Difference → requires relational carrier (ℂ²)
- SU(2) → yields spinorial behavior (720°)

**Missing link:**

> How does Θ (phase) generate SU(2) transformations from E₀ primitives?

---

## 2. Current state of Θ

Amplitude definition:

```text
Ψ(p) = exp(-S(p)) · exp(iΘ(p))
```

Observations:

- S(p) → scalar decay (stable)
- Θ(p) → accumulated phase (not fully derived)

Problem:

```text
exp(iΘ) ∈ U(1)
```

→ insufficient for internal difference

---

## 3. Key hypothesis

Θ is not a scalar — it is a **projection of a higher-dimensional generator**.

Formally:

```text
Θ = ∫ ω · dl
```

Where:

- ω is a rotational field (from v_rot)
- dl is path element

---

## 4. From scalar phase to generator

In U(1):

```text
U = exp(iΘ)
```

In SU(2), rotation is generated by:

```text
U = exp(-i Θ/2 · (n · σ))
```

Thus:

- Θ → magnitude of rotation
- n (direction) → axis of rotation

---

## 5. Source of n (rotation axis)

From E₀ decomposition:

```text
v = v_grad + v_rot
```

We identify:

```text
n = normalize(v_rot)
```

Thus:

- v_rot defines rotational structure
- direction of v_rot → axis of SU(2) rotation

---

## 6. Generator construction

Define generator:

```text
G = Θ · (n · σ)
```

Then propagator:

```text
U = exp(-i G/2)
```

---

## 7. Interpretation

Previously:

- Θ was treated as scalar phase

Now:

- Θ is magnitude of rotation in internal space
- v_rot gives orientation of that rotation

Thus:

```text
(Θ, v_rot) → full SU(2) generator
```

---

## 8. Path accumulation

For path p:

```text
U(p) = ∏ exp(-i Θ_e/2 · (n_e · σ))
```

This replaces:

```text
Ψ(p) = exp(i ΣΘ)
```

---

## 9. Key consequence

Loops now carry structure:

```text
Loop → nontrivial SU(2) holonomy
```

Thus:

- phase accumulation becomes geometric
- loops can induce sign inversion

---

## 10. Emergence of 720°

If loop corresponds to full rotation:

```text
U(2π) = -I
U(4π) = I
```

This emerges automatically from SU(2)

---

## 11. Critical distinction

This is NOT:

- adding SU(2)

This IS:

> lifting Θ from scalar to generator form

---

## 12. Testable consequences

1. Closed loop with Θ = 2π:
   → sign inversion

2. Two loops:
   → identity

3. Different loop orientations:
   → interference differences

---

## 13. Integration with controller

Replace:

```python
psi *= exp(i * theta)
```

With:

```python
psi = U(theta, v_rot) @ psi
```

---

## 14. Open questions

- exact derivation of Θ from primitives
- discretization stability
- numerical accumulation of SU(2)

---

## 15. Conclusion

Θ is not just a phase.

It is:

> the scalar shadow of a rotational generator.

When lifted with v_rot, it naturally becomes SU(2).

---

## End of Document
